<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.todev.samples.FenceActivity"
    >

  <Button
      android:id="@+id/time_fence_button"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:text="Time Fence"
      style="@style/Widget.AppCompat.Button.Colored"
      />

  <Button
      android:id="@+id/distance_fence_button"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:text="Distance fence"
      style="@style/Widget.AppCompat.Button.Colored"
      />

  <Button
      android:id="@+id/moving_fence_button"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:text="Moving fence"
      style="@style/Widget.AppCompat.Button.Colored"
      />

  <Button
      android:id="@+id/home_fence_button"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:text="Home fence"
      style="@style/Widget.AppCompat.Button.Colored"
      />

</LinearLayout>
